## Container Lifecycle 
1. `docker ps` – showing that my-nginx is up.
2. `docker stop my-nginx` – stops the running Nginx container.
3. `docker ps -a` – shows my-nginx with an "Exited" status.
4. `docker rm my-nginx` – Permanently removes the stopped container from the system.
